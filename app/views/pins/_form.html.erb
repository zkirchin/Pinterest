<%= form_for(@pin) do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :Select_Board %><br>
    <%= f.collection_select(:board_id, Board.all, :id, :name) %><br>
  </div>
  <div class="field">
    <%= f.label :Select_User %><br> 
    <%= f.collection_select(:user_id, User.all, :id, :full_name) %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
